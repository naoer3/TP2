<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tp2.mp.dj.tp2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_mem"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_mem_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClassementActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Jeu"/>
        <activity android:name=".Resultats" />
        <activity android:name=".PagePlay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>